// @ts-check
import { defineConfig } from 'astro/config';

export default defineConfig({
  // Uncomment and fill in for GitHub Pages deployment:
  // site: 'https://lennertgavel.github.io',
  // base: '/lennertgavel.github.io/',
  output: 'static',
  publicDir: 'assets',
});
